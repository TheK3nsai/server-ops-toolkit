# ops/.env - Configuration for ops scripts
# This file is gitignored - safe for secrets

# =============================================================================
# Notification Settings
# =============================================================================

# Email notifications (requires mailx or msmtp configured)
# ALERT_EMAIL="admin@example.com"

# Uptime Kuma push monitor URLs (create passive monitors in Uptime Kuma)
# Format: https://uptime.example.com/api/push/XXXXXXXXXX
# UPTIME_KUMA_AUTH_PUSH=""      # check-auth-logs.sh
# UPTIME_KUMA_F2B_PUSH=""       # fail2ban-status.sh
# UPTIME_KUMA_PORTS_PUSH=""     # open-ports-audit.sh
# UPTIME_KUMA_DISK_PUSH=""      # disk-usage-alert.sh
# UPTIME_KUMA_DOCKER_PUSH=""    # docker-health-check.sh
# UPTIME_KUMA_RESOURCES_PUSH="" # container-resource-report.sh
# UPTIME_KUMA_UPDATES_PUSH=""   # system-updates.sh
# UPTIME_KUMA_MEMORY_PUSH=""    # memory-swap-monitor.sh

# =============================================================================
# Thresholds
# =============================================================================

# Failed login attempts before flagging an IP as suspicious
# FAILED_LOGIN_THRESHOLD=5

# Disk usage percentage to trigger warning
# DISK_WARN_THRESHOLD=80

# Disk usage percentage to trigger critical alert
# DISK_CRIT_THRESHOLD=90

# Memory/swap thresholds (memory-swap-monitor.sh)
# MEM_WARN_THRESHOLD=80     # Memory percentage to trigger warning
# MEM_CRIT_THRESHOLD=90     # Memory percentage to trigger critical
# SWAP_WARN_THRESHOLD=50    # Swap percentage to trigger warning

# Container resource thresholds (container-resource-report.sh)
# CONTAINER_MEM_WARN=80    # Memory percentage to trigger warning
# CONTAINER_CPU_WARN=90    # CPU percentage to trigger warning

# Docker root directory (docker-health-check.sh)
# DOCKER_ROOT=/home/kensai/docker

# Inode usage warning threshold (disk-usage-alert.sh)
# INODE_WARN_THRESHOLD=80

# Backup settings (backup-configs.sh)
# BACKUP_DEST=/home/kensai/ops/backups
# BACKUP_RETENTION_DAYS=30

# Compliance/Audit settings (access-audit-report.sh)
# AUDIT_REPORT_DAYS=30    # Default report period

# =============================================================================
# External Services
# =============================================================================

# Zammad API for ticket creation (optional)
# ZAMMAD_URL="https://tickets.example.com"
# ZAMMAD_TOKEN=""

# Slack/Discord webhook for notifications (optional)
# SLACK_WEBHOOK_URL=""
# DISCORD_WEBHOOK_URL=""
